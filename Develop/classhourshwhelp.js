let psdLen = 10;
let numArr = ["0", "1", "2", "3", "4", "5", "6"];
let abcArr = ["a", "b", "c", "d", "e"];
let specCharArr = []
let combinedArr = numArr + abcArr;
let combinedArr = numArr.concat(abcArr).concat(specCharArr);


for (let i = 0; i < pwdLen; i++) {
    const element = combinedArr[Math.floor(Math.random() * numArr.length)];
    
}

for (let i = 0; i < numArr.length; i++) {
    const element = numArr[Math.floor(Math.random() * numArr.length)];
    
}

